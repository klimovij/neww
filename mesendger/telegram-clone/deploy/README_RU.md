# 🚀 Быстрый старт: Развертывание на Google Cloud

## Ваш сервер
- **IP адрес**: `34.136.222.226`
- **Зона**: `us-central1-c`
- **Имя инстанса**: `instance-20251115-102239`

## 📝 Краткая инструкция

### 1. Подключитесь к серверу

**Через Google Cloud Console:**
1. Откройте [Google Cloud Console](https://console.cloud.google.com/compute/instances)
2. Найдите ваш инстанс `instance-20251115-102239`
3. Нажмите кнопку **"SSH"** (откроется терминал в браузере)

**Или через командную строку:**
```bash
gcloud compute ssh instance-20251115-102239 --zone=us-central1-c
```

### 2. Загрузите файлы на сервер

**Вариант A: Через SCP (с вашего компьютера Windows)**

Откройте PowerShell на вашем компьютере и выполните:

```powershell
# Перейдите в директорию проекта
cd C:\Users\Ronin\Desktop\mesendger-good

# Загрузите файлы на сервер
scp -r mesendger/telegram-clone/* username@34.136.222.226:/tmp/mesendger/
```

**Вариант B: Через Git (рекомендуется)**

1. Создайте репозиторий на GitHub
2. Загрузите туда код
3. На сервере выполните:
```bash
cd /var/www
sudo git clone YOUR_REPO_URL mesendger
```

### 3. Запустите скрипт настройки

На сервере выполните:

```bash
# Перейдите в директорию проекта
cd /var/www/mesendger  # или /tmp/mesendger если загрузили туда

# Сделайте скрипты исполняемыми
chmod +x deploy/*.sh

# Запустите настройку сервера
sudo bash deploy/setup-server.sh

# Затем разверните приложение
sudo bash deploy/deploy-app.sh
```

### 4. Настройте файрвол в Google Cloud

1. Откройте [Google Cloud Console](https://console.cloud.google.com/networking/firewalls)
2. Создайте правила файрвола:
   - **HTTP**: Разрешить TCP порт 80
   - **HTTPS**: Разрешить TCP порт 443
   - **SSH**: Разрешить TCP порт 22 (обычно уже есть)

Или выполните команды:
```bash
gcloud compute firewall-rules create allow-http \
    --allow tcp:80 \
    --source-ranges 0.0.0.0/0

gcloud compute firewall-rules create allow-https \
    --allow tcp:443 \
    --source-ranges 0.0.0.0/0
```

### 5. Откройте приложение

Откройте в браузере: **http://34.136.222.226**

## 🔧 Полезные команды

```bash
# Проверка статуса приложения
pm2 status

# Просмотр логов
pm2 logs

# Перезапуск приложения
pm2 restart all

# Проверка nginx
sudo systemctl status nginx

# Просмотр логов nginx
sudo tail -f /var/log/nginx/mesendger-error.log
```

## ⚙️ Настройка переменных окружения

Отредактируйте файл `/var/www/mesendger/server/.env`:

```bash
sudo nano /var/www/mesendger/server/.env
```

Убедитесь, что там указано:
```env
PORT=5000
NODE_ENV=production
JWT_SECRET=ваш-секретный-ключ
EXTERNAL_URL=http://34.136.222.226
CORS_ORIGINS=http://34.136.222.226
```

## 🆘 Решение проблем

### Приложение не открывается

1. Проверьте файрвол в Google Cloud Console
2. Проверьте статус: `pm2 status`
3. Проверьте логи: `pm2 logs`

### Ошибки подключения

1. Убедитесь, что порты 80 и 443 открыты в Google Cloud
2. Проверьте nginx: `sudo systemctl status nginx`
3. Проверьте логи nginx: `sudo tail -f /var/log/nginx/error.log`

## 📚 Подробная инструкция

См. файл `deploy/DEPLOYMENT_GUIDE.md` для детальной информации.

---

**Готово!** После выполнения этих шагов ваше приложение будет доступно по адресу `http://34.136.222.226`

