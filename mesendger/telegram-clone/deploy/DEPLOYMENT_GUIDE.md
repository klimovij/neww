# Руководство по развертыванию мессенджера на Google Cloud

Это пошаговое руководство поможет вам развернуть приложение на виртуальном сервере Google Cloud Platform.

## 📋 Предварительные требования

- Виртуальный сервер в Google Cloud (уже создан)
- SSH доступ к серверу
- Внешний IP адрес: `34.136.222.226` (ваш текущий IP)

## 🚀 Шаг 1: Подключение к серверу

### Windows (PowerShell или CMD):

```powershell
# Скачайте и установите Google Cloud SDK, затем:
gcloud compute ssh instance-20251115-102239 --zone=us-central1-c

# Или используйте обычный SSH (если настроены SSH ключи):
ssh username@34.136.222.226
```

### Альтернативный способ (через браузер):

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Перейдите в Compute Engine → VM instances
3. Нажмите на вашу VM → "SSH" (откроется терминал в браузере)

## 🔧 Шаг 2: Первоначальная настройка сервера

После подключения к серверу выполните:

```bash
# Обновление системы
sudo apt-get update
sudo apt-get upgrade -y

# Установка необходимых пакетов
sudo apt-get install -y git curl

# Скачайте скрипт настройки (или скопируйте его содержимое)
# Выполните скрипт настройки
sudo bash setup-server.sh
```

Этот скрипт установит:
- Node.js 20.x
- npm
- PM2 (менеджер процессов)
- nginx (веб-сервер)
- Настроит файрвол

## 📦 Шаг 3: Загрузка кода на сервер

### Вариант A: Через Git (рекомендуется)

```bash
# Создайте репозиторий на GitHub/GitLab и загрузите туда код
# Затем на сервере:
cd /var/www
sudo git clone YOUR_REPOSITORY_URL mesendger
sudo chown -R appuser:appuser /var/www/mesendger
```

### Вариант B: Через SCP (с вашего компьютера)

```powershell
# В PowerShell на вашем компьютере:
scp -r mesendger/telegram-clone/* username@34.136.222.226:/tmp/mesendger-temp/

# Затем на сервере:
sudo mv /tmp/mesendger-temp/* /var/www/mesendger/
sudo chown -R appuser:appuser /var/www/mesendger
```

### Вариант C: Через Google Cloud Storage

```bash
# Загрузите архив на Google Cloud Storage, затем:
gsutil cp gs://YOUR_BUCKET/mesendger.tar.gz /tmp/
cd /var/www
sudo tar -xzf /tmp/mesendger.tar.gz
sudo chown -R appuser:appuser /var/www/mesendger
```

## ⚙️ Шаг 4: Настройка переменных окружения

```bash
cd /var/www/mesendger/server

# Создайте .env файл
sudo -u appuser nano .env
```

Добавьте следующее содержимое (замените значения на свои):

```env
PORT=5000
NODE_ENV=production
JWT_SECRET=your-very-secure-secret-key-change-this
EXTERNAL_URL=http://34.136.222.226
CORS_ORIGINS=http://34.136.222.226,http://localhost:3000,http://localhost:3001
GEMINI_API_KEYS=your-gemini-api-key-if-needed
```

**Важно:** Сгенерируйте безопасный JWT_SECRET:
```bash
openssl rand -base64 32
```

## 🏗️ Шаг 5: Установка зависимостей и сборка

```bash
cd /var/www/mesendger

# Установка зависимостей сервера
cd server
sudo -u appuser npm install --production

# Сборка React приложения
cd ../client-react
sudo -u appuser npm install
sudo -u appuser npm run build
```

## 🔄 Шаг 6: Настройка PM2

```bash
cd /var/www/mesendger

# Запуск через PM2
sudo -u appuser pm2 start deploy/ecosystem.config.js

# Сохранение конфигурации для автозапуска
sudo -u appuser pm2 save

# Настройка автозапуска при перезагрузке сервера
sudo -u appuser pm2 startup systemd -u appuser --hp /home/appuser
# Выполните команду, которую выведет PM2 (обычно что-то вроде):
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u appuser --hp /home/appuser
```

## 🌐 Шаг 7: Настройка Nginx

```bash
# Копируем конфигурацию nginx
sudo cp /var/www/mesendger/deploy/nginx.conf /etc/nginx/sites-available/mesendger

# Редактируем конфигурацию (замените YOUR_SERVER_IP_OR_DOMAIN на ваш IP)
sudo nano /etc/nginx/sites-available/mesendger
```

В файле замените `YOUR_SERVER_IP_OR_DOMAIN` на `34.136.222.226`:

```nginx
server_name 34.136.222.226;
```

```bash
# Создаем симлинк
sudo ln -s /etc/nginx/sites-available/mesendger /etc/nginx/sites-enabled/

# Удаляем дефолтную конфигурацию (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверяем конфигурацию
sudo nginx -t

# Перезапускаем nginx
sudo systemctl restart nginx
sudo systemctl enable nginx
```

## 🔥 Шаг 8: Настройка файрвола Google Cloud

В Google Cloud Console:

1. Перейдите в **VPC network** → **Firewall rules**
2. Создайте правила (или используйте существующие):
   - **HTTP**: Разрешить входящий трафик на порт 80 (TCP)
   - **HTTPS**: Разрешить входящий трафик на порт 443 (TCP)
   - **SSH**: Разрешить входящий трафик на порт 22 (TCP)

Или через gcloud CLI:
```bash
gcloud compute firewall-rules create allow-http \
    --allow tcp:80 \
    --source-ranges 0.0.0.0/0 \
    --description "Allow HTTP traffic"

gcloud compute firewall-rules create allow-https \
    --allow tcp:443 \
    --source-ranges 0.0.0.0/0 \
    --description "Allow HTTPS traffic"
```

## ✅ Шаг 9: Проверка работы

```bash
# Проверка статуса PM2
pm2 status

# Просмотр логов
pm2 logs

# Проверка nginx
sudo systemctl status nginx

# Проверка портов
sudo netstat -tlnp | grep -E ':(80|5000)'
```

Откройте в браузере: `http://34.136.222.226`

## 🔍 Устранение проблем

### Приложение не запускается

```bash
# Проверьте логи PM2
pm2 logs mesendger-server

# Проверьте логи nginx
sudo tail -f /var/log/nginx/mesendger-error.log

# Проверьте, запущен ли сервер
pm2 status
```

### Ошибки подключения

1. Проверьте файрвол Google Cloud
2. Проверьте, что порты открыты: `sudo ufw status`
3. Проверьте, что nginx запущен: `sudo systemctl status nginx`

### Проблемы с базой данных

```bash
# Проверьте права на базу данных
ls -la /var/www/mesendger/server/messenger.db
sudo chown appuser:appuser /var/www/mesendger/server/messenger.db
```

### Обновление приложения

```bash
cd /var/www/mesendger

# Обновите код (через git или scp)
git pull  # если используете git

# Переустановите зависимости (если нужно)
cd server && sudo -u appuser npm install --production
cd ../client-react && sudo -u appuser npm run build

# Перезапустите приложение
pm2 restart all
```

## 🔐 Шаг 10: Настройка SSL (опционально, но рекомендуется)

Для использования HTTPS установите Let's Encrypt:

```bash
# Установка Certbot
sudo apt-get install -y certbot python3-certbot-nginx

# Получение сертификата (замените на ваш домен или IP)
sudo certbot --nginx -d 34.136.222.226

# Автоматическое обновление
sudo certbot renew --dry-run
```

## 📊 Мониторинг

```bash
# Просмотр статистики PM2
pm2 monit

# Просмотр логов в реальном времени
pm2 logs --lines 100

# Перезапуск приложения
pm2 restart all

# Остановка приложения
pm2 stop all

# Удаление из PM2
pm2 delete all
```

## 🔄 Автоматическое развертывание

Для автоматического развертывания используйте скрипт:

```bash
cd /var/www/mesendger
sudo bash deploy/deploy-app.sh
```

Этот скрипт:
- Скопирует файлы
- Установит зависимости
- Соберет React приложение
- Запустит через PM2
- Перезапустит nginx

## 📝 Полезные команды

```bash
# Перезапуск сервера
sudo reboot

# Проверка использования диска
df -h

# Проверка использования памяти
free -h

# Просмотр активных процессов
ps aux | grep node

# Очистка логов PM2
pm2 flush
```

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs` и `sudo tail -f /var/log/nginx/error.log`
2. Убедитесь, что все порты открыты в Google Cloud
3. Проверьте, что переменные окружения настроены правильно
4. Убедитесь, что база данных доступна и имеет правильные права

---

**Готово!** Ваше приложение должно быть доступно по адресу `http://34.136.222.226`

